apiVersion: jiemi.cr/v1alpha1
kind: Decryptor
metadata:
  name: gcp-decryptor
spec:
  provider:
    gcp:
      secretName: gcp-service-account
      key: sa
  source:
    repository:
      url: https://github.com/shigedangao/uselesscli.git
      credentials: {}
    fileToDecrypt: dodo.enc.yaml
    sopsPath: .sops.yaml
